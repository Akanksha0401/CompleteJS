const age = 10;

console.log(age >= 18 ? "Eligible to vote" : "Not eligible to vote");
// ternary operator can also be passed inside template literal
console.log(`the result is ${age >= 18 ? "Eligible to vote" : "Not eligible to vote"}`)

