let a = 45;
//if (a === 45)
//   alert("You are done!");
console.log(40 + 23 + 12);
let fname = "Akanksha";
console.log(fname);
// only letters ,numbers, $ and _ is allowed while naming variables
let _fruit = "apple";
let $veg = "spinach";
let PI = 3.14; // it's a naming convention that we use all uppercase letters for name a variable that denotes a universal constant like PI etc.